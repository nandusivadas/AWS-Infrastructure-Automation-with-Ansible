---
- name: Install Apache
  dnf:
    name: httpd
    state: present

- name: Start Apache
  service:
    name: httpd
    state: started
    enabled: yes

- name: Deploy index page
  template:
    src: index.j2
    dest: /var/www/html/index.html
